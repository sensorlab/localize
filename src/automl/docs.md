# AutoML (AutoKeras)
The `automl` configuration section defines models on which to perform NAS using autokeras. AutoKeras allows the user to be completely hand-off by designing the architecture by itself while also allowing the user to specify the high level architecture.

## Model Definitions

The user can define custom models by specifying inputs, blocks, and outputs. Inputs and outputs are required, while blocks is optional

### Inputs



